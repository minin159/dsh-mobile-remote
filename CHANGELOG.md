# Changelog

## 1.3.0 (2026-09-02)

阶段 5 · 底栏运行时控件（5a 探针 + 5b 实现一个任务完成）。本轮按收紧范围交付：
**盾牌三档全做；P7–P9 控件不做完整实现**（探针结论已存档 `docs/probe-findings.md`，
完整实现留待以后按需再开）。

### 探针（5a，tag phase-5a-done）

- **P7 上下文用量/缓存命中——证实**：`assistant/message` 事件自带 `usage`（实测缓存命中
  `cacheReadTokens:1792`）；宿主 `tokenMeter` 三投影直出占用/容量。
- **P8 运行时切模型——证实（官方路径）**：`sessionController.selectModel` → `model/selection`
  会话事件（插件 append 实测回流）+ 下一请求生效 + 副作用同步全局默认；插件自改写
  `agent/request` 双样式实测未落地 → 未定，禁止依赖。
- **P9 思考档位——证实（机制）/ 按模型显隐**：档位目录 per-model 声明；本机
  GLM-5.3-flash 未声明（`reasoning=undefined`）→ 控件隐藏。

### 新增

- **🛡 盾牌 = 访问权限模式三档**（底栏键 + 弹层选档）：手机审批（默认）/ 全部放行 /
  全部拒绝。「全部放行」= 插件对绑定会话的审批自动回 `allowed-once`（P3 语义：插件决策
  即终局），页面红色警示条常驻 + 切换二次确认 + 键位红色高亮；「全部拒绝」= 自动回
  `rejected`。生效前提与手机审批一致（插件启用 + 手机在线 + 绑定会话），任一不满足
  自动回落电脑端 GUI（fail-safe：手机断线后代答不生效）。
- **档位为会话级临时状态**：仅存内存，插件重启或「停止远程」回到手机审批（停止远程
  回落也写审计）；每次切换写审计 JSONL（`shield_mode`），盾牌代答以
  `approval_decide via=shield-allow/shield-deny` 可回溯；手机端切换经 SSE `shield` 帧
  即时同步，重连/整页重载由 `hello` 帧兜底。
- **电脑端同步**：设置页新增「权限模式（盾牌）」状态行（只读），全部放行时红色显示。
- **◐ 上下文/缓存只读展示**（P7 最小形态，数据在既有 SSE 流上零新增通道）：键位显示
  最近一轮占用（input+cacheRead+cacheWrite）与缓存命中率，点击 toast 展示明细；
  超长帧被瘦身时保留最近已知值（防御式）。
- **↑ 发送**：底栏对齐 ZCode 控制条布局（输入框 · 🛡 · ◐ · ↑），发送沿用 2s 节流；
  **模型键与思考强度键本轮隐藏**（P8/P9 降级，见探针结论）；附件与调色板不做。

### 测试

- 新增 `probe/smoke-phase5.mjs`（19 项）：盾牌三档审批链路（自动放行/自动拒绝/回落
  手机审批）、断线 fail-safe、重连 hello 兜底、非法档位 400/错码 401、审计可回溯、
  页面控件形态断言。阶段 2 回归 26 项 + 阶段 4 回归 27 项全过。
- 已知小限制（阶段 2 既有实现，非本轮引入）：`audit()` 为 fire-and-forget appendFile，
  并发突发时行序不保证（冒烟测试已改为顺序无关断言）。

### 真机验收清单

盾牌三档各实测一条审批路径 + 审计回溯 + 设置页同步；◐ 只读展示与降级键位确认；
合并阶段 2 欠账六项 + 阶段 4 五项 + Tailscale 连通性补测（清单见 roadmap 阶段 5 行）。

## 1.2.0 (2026-09-02)

阶段 4 · UI 2.0：移动端信息架构对齐 ZCode webRemoteControl。90% 工作在 web/page.html，
后端仅 `/mobile-remote/sessions` 补一个字段。

### 新增

- **首页 = 工作区卡片长列表**（替换原「☰ 会话」平铺抽屉）：全量会话按 `header.cwd`
  分组，无 cwd 的会话归入「未分组」卡片（止损，不阻塞交付）；卡片头 = 工作区名
  （cwd 末段）+ 完整路径 + 任务数 + 最近更新相对时间 + 展开/收起（折叠状态
  localStorage 持久化，跨重载保持）；页首汇总行「N 个工作区 · M 个任务」+ 连接状态
  （"已连接到 DSH 实例"）+ 刷新按钮；首次进入说明卡（一次性可关闭，失效后回电脑端
  重新配对的指引）；手机端钉住会话时显示「跟随电脑当前会话」快捷入口。
  **不做新建会话「+」**：DSH 新建会话能力未探测，如实降级并记录在文档。
- **任务行**：标题截断 + 相对时间（最近活动 > 真人输入 > 创建时间）+ 四态状态胶囊
  （沿用阶段 2 映射）+ 点击进入会话视图（复用 `POST /switch` 切换绑定 + 钉住语义；
  点击已绑定会话直接切视图，不清流不打断）。
- **会话视图对齐**：顶栏 ← 返回首页 + 当前会话标题（标题快照优先，cwd/短 id 回退）；
  消息流新增 markdown **表格**渲染（表头/数据行/列数校验防误吞/窄屏横向滚动）与
  **行内代码 chip**，全程受控 DOM 构建（textContent / createTextNode，渲染路径零
  innerHTML），与阶段 2 代码块同防注入标准；流式期间悬浮**「当前动作」pill**
  （最新 tool/call 名称 + 参数摘要，收到 tool/result 或回合结束即收起，仅会话视图显示）；
  离开底部出现**回到底部 ↓ FAB**（点击回底，贴底自动隐藏）。
- **双视图导航**：首页 ↔ 会话双向滚动位置保持（会话视图按会话 id 记忆，往返不丢）；
  息屏整页重载回到离开时的视图（sessionStorage 记忆，补全 P5 主恢复路径的体验）；
  SSE 状态帧增量更新首页任务行胶囊与绑定高亮（无需整表重建），连接状态同时驱动
  顶栏与首页汇总行。

### 后端（最小改动）

- `/mobile-remote/sessions` 响应补 `lastAt`（每会话最近活动时间，自 activity 表透出，
  无记录时为 0、前端回退 createdAt）；`cwd` 阶段 2 已带。其余零改动。

### 质量基线

- `probe/smoke-phase4.mjs`：27 项断言——页面脚本在 vm + mini DOM 下跑真函数
  （表格/行内代码/防注入/围栏内不误判/分组与未分组排序/相对时间/renderHome 全链路），
  加 `/sessions` 的 `lastAt` 路由断言；阶段 2 冒烟 26 项回归同步通过。
- 浏览器实测（`probe/mock-phase4.mjs` mock 服务器 + 390×844 视口）：首页分组卡片/
  折叠记忆/任务行进入、表格与行内代码渲染、往返滚动保持（首页与长内容会话分别恢复）、
  重载视图记忆、「当前动作」pill 与 ↓FAB 时序，全部通过。

### 语义说明

- 状态四态沿用阶段 2 启发式，「完成」仍是 idle + 上一回合正常收尾的推断值。
- 底部输入栏保持现状不动：五个扩展键位（盾牌/上下文/模型/思考强度）留给阶段 5；
  附件「+」不做（steer 纯文本），调色板主题切换不做（跟随系统）。
- 1.1.0 升级：设置与配对完全兼容，页面首屏从消息流变为工作区卡片列表，
  点任务行即进入会话视图；停用态行为不变（零影响承诺不变）。
- 真机验收（合并阶段 2 欠账六项 + 本阶段五项）待手机配合逐项触发回填。

## 1.1.0 (2026-09-02)

阶段 2 · 完整对齐 ZCode 功能面。六项功能，每项独立 commit。

### 新增

- **会话列表与切换**：顶栏「☰ 会话」抽屉列出全量逻辑会话（live 优先、newest-first，
  标题经 `sessionQuery.readTitleSnapshots` 折叠 + 60s 缓存 + 单批上限 50，
  含 cwd/时间/状态/绑定标记）；点选即切换绑定（`POST /mobile-remote/switch`，
  live + 持久化两级存在性校验），「跟随电脑当前会话」回到自动挑选。
  转发策略升级：enabled 期间**所有根会话**事件入各自环形缓冲（16 会话 × 200 帧有界），
  绑定会话额外直发——切换后按连接级游标补发目标会话近期帧，不再出现"切换后空白流"，
  切回也不重复。新增手机端**钉住语义**：手动选定的会话跨重连/整页重载保持，
  不被自动挑选覆盖；停止远程/重置配对码时清除。首次进入页面也回放近期帧。
- **状态标签**：会话四态 运行中/空闲/完成/错误。`agent/status` running→运行中；
  idle 且上一回合 `turn/end` reason.kind==='completed'→完成（启发式，宿主无独立
  done 语义）；`agent/error`（阶段 1 未接线）→错误，真人新输入或新一轮运行时清除。
  顶栏 chip、会话抽屉、设置页状态行三处一致；错误首次出现 toast 提示。
- **配对过期与重配对**（对齐 ZCode 语义：过期需电脑端重新生成）：配对码有效期
  默认 72h（0=不过期，档位 不过期/24 小时/3 天/7 天）+ 签发时间（旧版升级自动回填）。
  全部端点 401 携带原因码 `bad-token`/`token-expired`；新增 `GET /mobile-remote/paircheck`
  供页面在 SSE 断开后探测失效原因（EventSource 拿不到 HTTP 状态码）——失效即停止
  自动重连并展示对应指引横幅；网络问题才走退避重连。重置配对码即重新起算有效期。
- **审计 JSONL**：`~/.dsh/mobile-remote-audit.jsonl` 逐行记录 page_open / connect /
  disconnect（含时长）/ replaced / stop / send（含 steer 耗时与成败）/
  approval_push / approval_decide（决定来源 + 等待耗时）/ switch（from/to/follow）/
  token_reset / enable / disable，含时间、动作、目标会话、来源、耗时、消息长度；
  **不记消息正文，token 一律不落盘**。默认开启，设置页可关（关闭后零写入）；
  写失败本轮自动停用；超 5MB 自动轮转 `.old`。
- **恢复加固**：服务端每 15s 发**具名 ping 事件**（注释行 EventSource 收不到，
  具名事件才是页面可感知的心跳）；客户端看门狗 45s 无心跳判定半开连接并主动重连
  （10s 周期检查）；重连退避 1s→10s 封顶叠加 ±30% 抖动；监听 `online` 事件网络
  恢复立即重连；服务端写帧失败立即拆除连接，挂起审批即时回落电脑端
  （审计标记 via:'write-error'）。
- **UI 打磨**：消息流 350 节点截断（「较早内容已折叠」提示，工具行索引同步清理）；
  最终消息按围栏渲染代码块（语言角标/横向滚动/等宽字体/暗色变量，全程
  textContent 构建防注入；流式期间保持纯文本优先性能）；工具行点击展开完整参数；
  发送失败分场景文案（会话结束→指引切换、节流→剩余秒数、未绑定→指引选会话）；
  输入框占位随绑定状态变化；选中色与滚动条暗色细节。

### 兼容与语义说明

- 状态「完成」为启发式映射：idle + 上一回合正常收尾（宿主未提供独立 done 语义）。
- 会话列表包含历史（仅持久化）会话：切换过去只能看到切换后的新事件（无 live 事件流），
  页面会提示「该会话未在运行」。
- 1.0.0 升级：已有配对码自动回填签发时间（有效期自升级时刻起算），其余设置不变；
  停用态行为与 1.0.0 完全一致（零影响承诺不变）。

### 质量基线

- `probe/smoke-phase2.mjs`：mock ctx 全路由冒烟回归 26 项断言通过（页面/配对过期/
  sessions/switch/SSE 补发/status 帧/send 全链路/审计/停用零影响），
  不依赖 DSH 宿主可随时重跑；并借此发现并修正 schema 缺席时 seed.enabled 被忽略的隐患。
- 真机验收（列表切换/状态刷新/过期重配对/审计回溯/息屏恢复/停用回归）待手机配合逐项触发。

## 1.0.0 (2026-09-02)

阶段 1 · MVP：单会话远程控制。首个发布版本。

### 新增

- **配对入口**：DSH 设置页「移动端远程」——开关、双地址显示（电脑回环调试 / 手机访问）、
  客户端 canvas 二维码（vendored MIT qrcode-generator，零依赖）、复制链接、
  重置配对码（旧链接立即失效）、停止远程。
- **页面与通道**：`/mobile-remote/p/<token>` prefix 路由内联返回移动端单页
  （零构建 vanilla JS + EventSource，本地资源、暗色跟随系统、安全区适配）；
  SSE 长连接 + POST 控制接口（P1 探针选型）；断线指数退避重连 1s→10s。
- **实时看流**：转发 `session/event`（user/message、assistant/chunk 流式增量、
  assistant/message、tool/call、tool/result、回合边界、审批审计）与 `agent/status`；
  聊天气泡渲染、滚动跟随、新消息提示条。
- **手机发消息**：`agent.steer` 带 `source:{kind:'plugin',plugin:'mobile-remote'}` 来源标记；
  服务端 + 页面双端发送节流（默认 2s，防连点，非宿主限制）。
- **手机审批**：拦截 `approval/request`，页面审批条（工具名+摘要+倒计时）；
  允许/拒绝即终局（P3 实测：GUI 不再弹）；插件自带超时（默认 120s，宿主无默认超时）
  到点 `next()` 回落电脑端 GUI；手机断开 10s 宽限后同样回落；
  `mobile_remote_selftest` 诊断工具用于自测三条审批路径。
- **安全**：token 即密码（随机 32 位 hex、日志打码、timingSafeEqual）；
  单连接绑定（新连接顶替并通知旧端）；设置接口仅限电脑回环，且在中继层二次封禁；
  环形缓冲（200 条/会话）+ `?since=` 游标补发（P5 实测：安卓息屏回前台走整页重载，
  该路径为主恢复链路）。
- 断点续传游标带服务器启动纪元（epoch），DSH 重启后页面自动重置游标，避免永久漏发。
- 会话绑定只认真人键盘输入（`user/message` 且 `source.kind==='user'`）——失败重试循环
  的会话事件流会永久霸占"最近活跃"绑定（实测踩坑），插件 steer 与后台事件不参与判定。

### 修正（相对阶段 0 规格的实测变更）

- **部署前提**：`dsh web --host 0.0.0.0` 被 DSH CLI 显式封禁（安全策略：避免把整个
  Agent 座舱暴露到网络；webserver config schema 亦只允许回环/0.0.0.0）。改为插件内置
  **路径过滤反向代理**：默认 `0.0.0.0:3090 → 127.0.0.1:3080`，仅放行 `/mobile-remote/*`，
  DSH 本体保持回环——比整站暴露更安全，手机地址由设置页自动给出。
- 审批等待窗可调档位为 30 秒/1/2/5 分钟（默认 120 秒），超时回落电脑端 GUI。

### 部署前提

- DSH web 正常启动即可（本体保持回环 127.0.0.1:3080）；手机可达性由插件中继承担，
  首次启用若被 Windows 防火墙拦截需放行 node 入站（局域网）。
