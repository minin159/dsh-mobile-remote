# Changelog

## 1.7.0 (2026-09-03)

C1 · 真机反馈修复轮（四条，基于 v1.6.0）。每条独立 commit，全部落地：
`f2e020d`（新建会话默认 D 盘）→ `f31f432`（思考可折叠块）→ `55c28c1`
（进会话回读历史）→ `01c0089`（APK 滚动失效）。**优4（多设备观察者
模式）按用户指示本轮不执行**，WIP 继续留存 `opt4-wip` 分支。

### 修复

- **新建会话默认工作区改 D 盘**：`/new` 不再沿用当前绑定会话目录，默认
  `NEW_SESSION_CWD = 'D:\'`（常量收口，本轮不做设置页配置项）；请求体显式
  `cwd` 仍以请求为准（预留官方参数面）。
- **APK 内消息显示不全、无法滑动（阻塞级）**：根因为壳沉浸式全屏
  （`SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN`）下 WebView 视口 = 整块物理屏，
  `html/body` 的 `height:100%` 基准超可视区 → `#stream` flex:1 分到超视口
  高度 → 溢出不发生 → `overflow-y:auto` 不触发，尾部被系统栏遮挡（浏览器
  App 自行扣系统栏故正常）。页面侧三层加固：`@supports 100dvh` 升级高度
  基准（老内核回退 100%）+ `body overflow:hidden` 掐断双滚动链 + dvh 实效
  探测失败时 `position:fixed` 兜底。壳侧 patch（adjustResize 等）见
  `docs/fix4-scroll-diagnosis.md`，按跨仓库约束交回主会话执行。

### 新增

- **思考过程可折叠块**：`reasoning-delta` 从一行「🤔 思考中…」提示升级为
  可折叠块——流式期间实时累积文本（展开态才自动滚底，收起态不打扰阅读
  位置），摘要行实时显示行数/字数；`assistant/message` 定稿后保留为收起态
  （定稿带 reasoning 字段以定稿为准，否则保留流式累积），点击随时展开。
  折叠偏好 localStorage 持久化（展开过则后续流式块默认展开）。载荷形状经
  宿主源码核实：reasoning 块与 reasoning-delta chunk 同出 BlockAssembler
  （pi-ai thinking_* 全链映射），模型不输出思考时不出现任何块。
- **进入会话回读最近历史**：接入 `sessionQuery.readSession`（P4 只探过
  存在性，本轮先核实载荷：`{session, events[]}`，live 优先、含全量事件，
  持久化经 chunk-row 解码还原）。SSE 建连与 `/switch` 时环形缓冲不足
  30 帧则拉最近 30 条定稿消息（过滤 chunk 噪声），独立 `history` SSE 帧
  推送；页面渲染为顶部「—— 历史 ——」分隔条 + 定稿消息（含思考块），
  与 live 流无缝衔接，决策 12 的游标补发语义不变；重复/迟到帧去重。
  读失败/宿主缺失静默降级为现状（仅缓冲回放），不阻塞连接。

### 质量基线

- 冒烟四套全绿：phase2 26 + phase4 69 + phase5 19 + opt2 19 = **133 断言 0 FAIL**。
  新增覆盖：/new 默认 cwd 断言改新语义（E4b/E5 + E5b 显式 cwd）；思考块 D17
  组 11 项（累积/摘要/定稿覆盖/空块移除/展开记忆/清理）；历史 E6 组 7 项
  （触发条件/chunk 过滤封顶/尾部对齐/读失败降级/无服务降级）+ D18 组 8 项
  （分隔条/顺序/思考块/去重/迟到/清理）。mini DOM stub 升级：`parentNode`
  维护、`insertBefore` 参考节点、`id` 赋值注册（getElementById 真实语义）。
- 载荷形状核实记录（宿主源码实查，防再次盲探）：reasoning-delta chunk
  `{type:'reasoning-delta', index, text}`；定稿 content 数组含
  `{type:'reasoning', text}`；readSession 见上。

### 待办（真机验收清单，待手机配合回填）

1. 新建会话落在 `D:\`（电脑端核对新会话 cwd）。
2. 思考块可展开/收起，定稿后保留且摘要正确。
3. 进入会话可见历史分隔条 + 历史 30 条，与 live 新消息无缝衔接。
4. APK 内长对话可滑动到底、输入框不被系统栏遮挡；**壳侧 patch 未执行前
   页面侧三层加固应已生效（壳只加载 URL，无需重装 APK 即可复验）**。
5. 浏览器端无回归（滚动/思考块/历史区）。

## 1.6.0 (2026-09-03)

优化任务 C · 内容层 Apple 化（优6）。移动端页面按「风格 E · iOS 黑灰」整体换肤，与 App 壳
一体（视觉基准 `mobile-remote-app/docs/ui-mockup-v5-ios-blackgray.png` 第五列）。
**纯 CSS 换皮零逻辑改动**：不动事件逻辑/协议字段/SSE 处理，不改 DOM 结构与 class 命名
（冒烟按 id/class 断言锁死），不碰 index.js/client.js。

### 变更（全部为样式层）

- **设计令牌替换**：背景 `#050506` / 卡片 `#141416` / 次级面 `#1F1F23` / 正文 `#F2F2F7` /
  次级灰 `#98989E` / 分割线 `#222226`·`#26262A`；主强调 = 白底黑字（运行中胶囊、发送键、
  新建/刷新主按钮、分段选中态、审批「允许」）；功能色仅开关绿 `#30D158` 与错误红 `#FF453A`；
  `theme-color` 同步 `#050506`。
- **字体栈切 SF**：`-apple-system / BlinkMacSystemFont / SF Pro Text / PingFang SC`，
  按钮/输入框显式 `font-family: inherit`（修掉按钮吃系统默认字体）；代码用 SF Mono 栈。
- **首页**：页首 iOS 大标题（26px/700）+ 副标题；工作区卡片 → inset grouped 形态
  （去描边、行内细分割线 `#222226`、按压态、chevron 折叠不变）；分段切换改 iOS segmented；
  说明卡/「跟随电脑」行/刷新同风格，「＋新建」改白底黑字主按钮。
- **会话视图**：AI 消息改无边框纯文本（`#ECECF0`），用户消息 `#2C2C2E` 深灰气泡右对齐；
  代码块/表格/行内代码 chip 圆角卡片化；工具行统一卡片态。
- **审批条**：允许/拒绝重绘为 50px 高、12px 圆角按钮（允许 = 白底黑字主按钮，拒绝 = 错误红实底）。
- **毛玻璃化**：↓FAB、「当前动作」pill、toast、顶栏、输入栏改半透明 + `backdrop-filter` blur。
- **底栏**：盾牌/模型键换 SF 风格细线条图标（CSS mask 内联 SVG，`@supports` 不支持 mask 时
  回退原 emoji 字形）；发送键白底黑字圆形；键位布局与 `↑`/`aria-label` 契约不变（冒烟 5.1b 锁死）。
- **亮色模式**：仅令牌翻转级简单适配（`#F2F2F7` 底），按 spec 不追求双主题打磨。

### 质量基线

- 冒烟四套全绿：phase2 26 + phase4 42 + phase5 19 + opt2 19 = **106 断言 0 FAIL**
  （page.html 脚本在 vm + mini DOM 全量跑通，id/class 契约零变动）。
- 浏览器 mock 实测（`probe/mock-phase4.mjs`，390×844 视口）：首页/会话/底栏三屏渲染确认，
  盾牌与模型 mask 细线条图标渲染正常。前后对比图：
  `docs/compare-opt6-before.png` / `docs/compare-opt6-after.png`。

### 待办

- 真机验收（约 20 分钟）待手机配合回填：三屏与壳一体无跳色、长列表滚动流畅、暗色一致性、
  停用零影响回归；并入下一任务「大验收 + 整改」。

## 1.5.0 (2026-09-03)

优化任务 B 第一项 · 会话控制（优2 模型切换真控件）。**优4（多设备观察者模式）按用户指示暂缓**，
已实现部分保留在 `opt4-wip` 分支，不随本版本发布。模型切换只走 P8 证实的官方路径；
插件自实现 waterfall 改写在 P8 实测未落地，禁止尝试。

### 新增

- **模型切换真控件（优2）**：会话视图底栏新增 🧊 模型键——`/sessions` 新增 `canModel` 能力位
  驱动显隐（与「＋新建」同款运行时探测：宿主缺 `sessionController.selectModel` 即隐藏，
  POST 返回 501 `switch-unavailable`，不静默失败）。点击弹出底部弹层：
  - 模型目录按供应商分组，数据源为官方 `llm.listProviders()` + `llm.listModels()`
    （条目形状防御归一为 `{id, name}`，兼容字符串形态）；
  - 当前生效模型高亮：读 `session.requestHeader().config`（P8 实测与消息 source 同源），
    读不到（会话结束/代理缺失）时显示「跟随电脑端默认」；
  - 点选即 `POST /mobile-remote/model` → 官方 `sessionController.selectModel({sessionId, provider, model})`，
    自下一回合生效（不打断进行中的回合）；宿主校验拒绝（模型不存在等）如实透传 502 + 原始信息。
  - 范围收紧（任务约定）：不做自定义模型名输入、不做批量/多会话生效、不做模型参数编辑。
- **审计 `model_switch`**：每次切换（含失败尝试）写 `~/.dsh/mobile-remote-audit.jsonl`，
  含 from/to（provider/model）与绑定会话 id，可回溯。

### 质量基线

- `probe/smoke-opt2.mjs`：19 项断言（目录形状归一/当前模型来源/官方调用契约/失败透传/
  501 能力缺失/停用零影响/读不到模型时 current=null）。
- `smoke-phase5` 5.1c 同步翻转（模型键由「探针降级隐藏」改为「真控件」断言）；
  smoke-phase2（26）/phase4（42）/phase5（19）回归全绿。

### 兼容与语义说明

- 官方路径的已知副作用：`selectModel` 会把选择**同步保存为电脑端全局默认模型**
  （P8 源码实证 `agentDefaultModel.saveSelection`，失败仅告警不影响切换），弹层内已明示；
  不希望改默认时在电脑端切回即可。
- 模型目录为适配器「建议目录」：宿主语义允许适配器接受未列出的模型 id；弹层仅列
  官方接口返回的模型集（不做自定义输入，任务范围）。
- 思考强度（🧠）键维持 P9 结论隐藏：本机 GLM-5.3-flash 未声明档位目录。

## 1.4.0 (2026-09-03)

优化任务 A · 入口与会话组织（优1 + 优3）。前置探针 P10 证实 DSH 官方创建会话 API，
新建会话按「证实」形态交付；时间线为纯前端重排，后端仅新增 /new 与 /qr.svg 两个路由。

### 探针（P10，随本版本存档）

- **P10 创建会话 API——证实**：`ctx.get('sessionController').create({cwd})`（生产 web UI
  「新建会话」同一后端）可由插件直调：返回 `{sessionId}`、新会话立即进入 live 注册表
  （`sessions.list()` / `agents.get(sid)` / `sessionQuery` 三路核对一致）、发出 `session/created`；
  **全新空闲会话可被既有 steer 路径唤醒出首轮**（实测 5s 内 assistant/message）。
  结论与证据见 `docs/probe-findings.md` P10 节，探针在 `probe/plugin10/`。

### 新增

- **pair_qr 工具（优1 桌面一键出码）**：在任意会话里让模型调用 `pair_qr` 即出配对卡片——
  markdown 图片行指向回环 `/mobile-remote/qr.svg?token=`（桌面端渲染为大尺寸二维码 SVG，
  P6 同构 vendored qrcode-generator 服务端出码）+ 完整链接文本 + 等宽 ASCII 兜底码。
  新增路由 `/mobile-remote/qr.svg`（token 即门禁，与设置页二维码同内容同级别）；
  vendored 库移至 `vendor/qrcode-generator.cjs`（MIT 头保留，零依赖）。每次出码写审计 `pair_qr`。
- **首页时间线视图（优3a）**：首页顶部「按工作区 / 按时间线」分段切换（localStorage 记忆）；
  时间线按最近活动倒序 + 自然日分组（今天/昨天/周X/完整日期），日期分区卡可折叠；
  复用任务行构建与 SSE 状态/绑定增量更新通道，**后端零改动**。
- **新建会话（优3b）**：首页汇总行「＋ 新建」按钮（仅当 `/sessions` 报告 `canNew` 时显示——
  运行时探测 `sessionController.create` 可用性，P10 未证实形态即自动隐藏，不阻塞其余功能）。
  `POST /mobile-remote/new`：以当前绑定会话的 cwd 调 `sessionController.create` → 自动切换
  绑定并钉住（决策 13）→ 页面切入会话视图，发首条消息即走既有 steer 路径唤醒全新会话。
  宿主缺失该服务时返回 501 `new-unavailable`（如实报错，不静默失败）；每次写审计 `session_new`。

### 取舍说明

- 配对链接全文随 pair_qr 出码进入会话日志（模型回复需含图片行/链接）——与设置页展示同级别
  的既定取舍；会话日志不出本机。
- 不做（本轮明确排除）：搜索/筛选/会话归档删除、系统托盘、桌面快捷方式、设置页二维码改动。

### 自检与验收

- smoke-phase4 扩至 42 项（时间线 D14–D16 共 9 项 + /new E3–E5 共 5 项），
  phase2 26 项 + phase5 19 项回归全绿；`node --check` 通过。
- 真机验收清单（待手机配合回填）：① 会话内调 pair_qr 出码 → 手机扫码可进；
  ② 时间线切换数据与工作区视图一致、分段记忆跨重载保持；③ ＋新建 → 手机发首条消息
  成功且桌面端可见新会话；④ 停用零影响回归。


## 1.3.0 (2026-09-02)

阶段 5 · 底栏运行时控件（5a 探针 + 5b 实现一个任务完成）。本轮按收紧范围交付：
**盾牌三档全做；P7–P9 控件不做完整实现**（探针结论已存档 `docs/probe-findings.md`，
完整实现留待以后按需再开）。

### 探针（5a，tag phase-5a-done）

- **P7 上下文用量/缓存命中——证实**：`assistant/message` 事件自带 `usage`（实测缓存命中
  `cacheReadTokens:1792`）；宿主 `tokenMeter` 三投影直出占用/容量。
- **P8 运行时切模型——证实（官方路径）**：`sessionController.selectModel` → `model/selection`
  会话事件（插件 append 实测回流）+ 下一请求生效 + 副作用同步全局默认；插件自改写
  `agent/request` 双样式实测未落地 → 未定，禁止依赖。
- **P9 思考档位——证实（机制）/ 按模型显隐**：档位目录 per-model 声明；本机
  GLM-5.3-flash 未声明（`reasoning=undefined`）→ 控件隐藏。

### 新增

- **🛡 盾牌 = 访问权限模式三档**（底栏键 + 弹层选档）：手机审批（默认）/ 全部放行 /
  全部拒绝。「全部放行」= 插件对绑定会话的审批自动回 `allowed-once`（P3 语义：插件决策
  即终局），页面红色警示条常驻 + 切换二次确认 + 键位红色高亮；「全部拒绝」= 自动回
  `rejected`。生效前提与手机审批一致（插件启用 + 手机在线 + 绑定会话），任一不满足
  自动回落电脑端 GUI（fail-safe：手机断线后代答不生效）。
- **档位为会话级临时状态**：仅存内存，插件重启或「停止远程」回到手机审批（停止远程
  回落也写审计）；每次切换写审计 JSONL（`shield_mode`），盾牌代答以
  `approval_decide via=shield-allow/shield-deny` 可回溯；手机端切换经 SSE `shield` 帧
  即时同步，重连/整页重载由 `hello` 帧兜底。
- **电脑端同步**：设置页新增「权限模式（盾牌）」状态行（只读），全部放行时红色显示。
- **◐ 上下文/缓存只读展示**（P7 最小形态，数据在既有 SSE 流上零新增通道）：键位显示
  最近一轮占用（input+cacheRead+cacheWrite）与缓存命中率，点击 toast 展示明细；
  超长帧被瘦身时保留最近已知值（防御式）。
- **↑ 发送**：底栏对齐 ZCode 控制条布局（输入框 · 🛡 · ◐ · ↑），发送沿用 2s 节流；
  **模型键与思考强度键本轮隐藏**（P8/P9 降级，见探针结论）；附件与调色板不做。

### 测试

- 新增 `probe/smoke-phase5.mjs`（19 项）：盾牌三档审批链路（自动放行/自动拒绝/回落
  手机审批）、断线 fail-safe、重连 hello 兜底、非法档位 400/错码 401、审计可回溯、
  页面控件形态断言。阶段 2 回归 26 项 + 阶段 4 回归 27 项全过。
- 已知小限制（阶段 2 既有实现，非本轮引入）：`audit()` 为 fire-and-forget appendFile，
  并发突发时行序不保证（冒烟测试已改为顺序无关断言）。

### 真机验收清单

盾牌三档各实测一条审批路径 + 审计回溯 + 设置页同步；◐ 只读展示与降级键位确认；
合并阶段 2 欠账六项 + 阶段 4 五项 + Tailscale 连通性补测（清单见 roadmap 阶段 5 行）。

## 1.2.0 (2026-09-02)

阶段 4 · UI 2.0：移动端信息架构对齐 ZCode webRemoteControl。90% 工作在 web/page.html，
后端仅 `/mobile-remote/sessions` 补一个字段。

### 新增

- **首页 = 工作区卡片长列表**（替换原「☰ 会话」平铺抽屉）：全量会话按 `header.cwd`
  分组，无 cwd 的会话归入「未分组」卡片（止损，不阻塞交付）；卡片头 = 工作区名
  （cwd 末段）+ 完整路径 + 任务数 + 最近更新相对时间 + 展开/收起（折叠状态
  localStorage 持久化，跨重载保持）；页首汇总行「N 个工作区 · M 个任务」+ 连接状态
  （"已连接到 DSH 实例"）+ 刷新按钮；首次进入说明卡（一次性可关闭，失效后回电脑端
  重新配对的指引）；手机端钉住会话时显示「跟随电脑当前会话」快捷入口。
  **不做新建会话「+」**：DSH 新建会话能力未探测，如实降级并记录在文档。
- **任务行**：标题截断 + 相对时间（最近活动 > 真人输入 > 创建时间）+ 四态状态胶囊
  （沿用阶段 2 映射）+ 点击进入会话视图（复用 `POST /switch` 切换绑定 + 钉住语义；
  点击已绑定会话直接切视图，不清流不打断）。
- **会话视图对齐**：顶栏 ← 返回首页 + 当前会话标题（标题快照优先，cwd/短 id 回退）；
  消息流新增 markdown **表格**渲染（表头/数据行/列数校验防误吞/窄屏横向滚动）与
  **行内代码 chip**，全程受控 DOM 构建（textContent / createTextNode，渲染路径零
  innerHTML），与阶段 2 代码块同防注入标准；流式期间悬浮**「当前动作」pill**
  （最新 tool/call 名称 + 参数摘要，收到 tool/result 或回合结束即收起，仅会话视图显示）；
  离开底部出现**回到底部 ↓ FAB**（点击回底，贴底自动隐藏）。
- **双视图导航**：首页 ↔ 会话双向滚动位置保持（会话视图按会话 id 记忆，往返不丢）；
  息屏整页重载回到离开时的视图（sessionStorage 记忆，补全 P5 主恢复路径的体验）；
  SSE 状态帧增量更新首页任务行胶囊与绑定高亮（无需整表重建），连接状态同时驱动
  顶栏与首页汇总行。

### 后端（最小改动）

- `/mobile-remote/sessions` 响应补 `lastAt`（每会话最近活动时间，自 activity 表透出，
  无记录时为 0、前端回退 createdAt）；`cwd` 阶段 2 已带。其余零改动。

### 质量基线

- `probe/smoke-phase4.mjs`：27 项断言——页面脚本在 vm + mini DOM 下跑真函数
  （表格/行内代码/防注入/围栏内不误判/分组与未分组排序/相对时间/renderHome 全链路），
  加 `/sessions` 的 `lastAt` 路由断言；阶段 2 冒烟 26 项回归同步通过。
- 浏览器实测（`probe/mock-phase4.mjs` mock 服务器 + 390×844 视口）：首页分组卡片/
  折叠记忆/任务行进入、表格与行内代码渲染、往返滚动保持（首页与长内容会话分别恢复）、
  重载视图记忆、「当前动作」pill 与 ↓FAB 时序，全部通过。

### 语义说明

- 状态四态沿用阶段 2 启发式，「完成」仍是 idle + 上一回合正常收尾的推断值。
- 底部输入栏保持现状不动：五个扩展键位（盾牌/上下文/模型/思考强度）留给阶段 5；
  附件「+」不做（steer 纯文本），调色板主题切换不做（跟随系统）。
- 1.1.0 升级：设置与配对完全兼容，页面首屏从消息流变为工作区卡片列表，
  点任务行即进入会话视图；停用态行为不变（零影响承诺不变）。
- 真机验收（合并阶段 2 欠账六项 + 本阶段五项）待手机配合逐项触发回填。

## 1.1.0 (2026-09-02)

阶段 2 · 完整对齐 ZCode 功能面。六项功能，每项独立 commit。

### 新增

- **会话列表与切换**：顶栏「☰ 会话」抽屉列出全量逻辑会话（live 优先、newest-first，
  标题经 `sessionQuery.readTitleSnapshots` 折叠 + 60s 缓存 + 单批上限 50，
  含 cwd/时间/状态/绑定标记）；点选即切换绑定（`POST /mobile-remote/switch`，
  live + 持久化两级存在性校验），「跟随电脑当前会话」回到自动挑选。
  转发策略升级：enabled 期间**所有根会话**事件入各自环形缓冲（16 会话 × 200 帧有界），
  绑定会话额外直发——切换后按连接级游标补发目标会话近期帧，不再出现"切换后空白流"，
  切回也不重复。新增手机端**钉住语义**：手动选定的会话跨重连/整页重载保持，
  不被自动挑选覆盖；停止远程/重置配对码时清除。首次进入页面也回放近期帧。
- **状态标签**：会话四态 运行中/空闲/完成/错误。`agent/status` running→运行中；
  idle 且上一回合 `turn/end` reason.kind==='completed'→完成（启发式，宿主无独立
  done 语义）；`agent/error`（阶段 1 未接线）→错误，真人新输入或新一轮运行时清除。
  顶栏 chip、会话抽屉、设置页状态行三处一致；错误首次出现 toast 提示。
- **配对过期与重配对**（对齐 ZCode 语义：过期需电脑端重新生成）：配对码有效期
  默认 72h（0=不过期，档位 不过期/24 小时/3 天/7 天）+ 签发时间（旧版升级自动回填）。
  全部端点 401 携带原因码 `bad-token`/`token-expired`；新增 `GET /mobile-remote/paircheck`
  供页面在 SSE 断开后探测失效原因（EventSource 拿不到 HTTP 状态码）——失效即停止
  自动重连并展示对应指引横幅；网络问题才走退避重连。重置配对码即重新起算有效期。
- **审计 JSONL**：`~/.dsh/mobile-remote-audit.jsonl` 逐行记录 page_open / connect /
  disconnect（含时长）/ replaced / stop / send（含 steer 耗时与成败）/
  approval_push / approval_decide（决定来源 + 等待耗时）/ switch（from/to/follow）/
  token_reset / enable / disable，含时间、动作、目标会话、来源、耗时、消息长度；
  **不记消息正文，token 一律不落盘**。默认开启，设置页可关（关闭后零写入）；
  写失败本轮自动停用；超 5MB 自动轮转 `.old`。
- **恢复加固**：服务端每 15s 发**具名 ping 事件**（注释行 EventSource 收不到，
  具名事件才是页面可感知的心跳）；客户端看门狗 45s 无心跳判定半开连接并主动重连
  （10s 周期检查）；重连退避 1s→10s 封顶叠加 ±30% 抖动；监听 `online` 事件网络
  恢复立即重连；服务端写帧失败立即拆除连接，挂起审批即时回落电脑端
  （审计标记 via:'write-error'）。
- **UI 打磨**：消息流 350 节点截断（「较早内容已折叠」提示，工具行索引同步清理）；
  最终消息按围栏渲染代码块（语言角标/横向滚动/等宽字体/暗色变量，全程
  textContent 构建防注入；流式期间保持纯文本优先性能）；工具行点击展开完整参数；
  发送失败分场景文案（会话结束→指引切换、节流→剩余秒数、未绑定→指引选会话）；
  输入框占位随绑定状态变化；选中色与滚动条暗色细节。

### 兼容与语义说明

- 状态「完成」为启发式映射：idle + 上一回合正常收尾（宿主未提供独立 done 语义）。
- 会话列表包含历史（仅持久化）会话：切换过去只能看到切换后的新事件（无 live 事件流），
  页面会提示「该会话未在运行」。
- 1.0.0 升级：已有配对码自动回填签发时间（有效期自升级时刻起算），其余设置不变；
  停用态行为与 1.0.0 完全一致（零影响承诺不变）。

### 质量基线

- `probe/smoke-phase2.mjs`：mock ctx 全路由冒烟回归 26 项断言通过（页面/配对过期/
  sessions/switch/SSE 补发/status 帧/send 全链路/审计/停用零影响），
  不依赖 DSH 宿主可随时重跑；并借此发现并修正 schema 缺席时 seed.enabled 被忽略的隐患。
- 真机验收（列表切换/状态刷新/过期重配对/审计回溯/息屏恢复/停用回归）待手机配合逐项触发。

## 1.0.0 (2026-09-02)

阶段 1 · MVP：单会话远程控制。首个发布版本。

### 新增

- **配对入口**：DSH 设置页「移动端远程」——开关、双地址显示（电脑回环调试 / 手机访问）、
  客户端 canvas 二维码（vendored MIT qrcode-generator，零依赖）、复制链接、
  重置配对码（旧链接立即失效）、停止远程。
- **页面与通道**：`/mobile-remote/p/<token>` prefix 路由内联返回移动端单页
  （零构建 vanilla JS + EventSource，本地资源、暗色跟随系统、安全区适配）；
  SSE 长连接 + POST 控制接口（P1 探针选型）；断线指数退避重连 1s→10s。
- **实时看流**：转发 `session/event`（user/message、assistant/chunk 流式增量、
  assistant/message、tool/call、tool/result、回合边界、审批审计）与 `agent/status`；
  聊天气泡渲染、滚动跟随、新消息提示条。
- **手机发消息**：`agent.steer` 带 `source:{kind:'plugin',plugin:'mobile-remote'}` 来源标记；
  服务端 + 页面双端发送节流（默认 2s，防连点，非宿主限制）。
- **手机审批**：拦截 `approval/request`，页面审批条（工具名+摘要+倒计时）；
  允许/拒绝即终局（P3 实测：GUI 不再弹）；插件自带超时（默认 120s，宿主无默认超时）
  到点 `next()` 回落电脑端 GUI；手机断开 10s 宽限后同样回落；
  `mobile_remote_selftest` 诊断工具用于自测三条审批路径。
- **安全**：token 即密码（随机 32 位 hex、日志打码、timingSafeEqual）；
  单连接绑定（新连接顶替并通知旧端）；设置接口仅限电脑回环，且在中继层二次封禁；
  环形缓冲（200 条/会话）+ `?since=` 游标补发（P5 实测：安卓息屏回前台走整页重载，
  该路径为主恢复链路）。
- 断点续传游标带服务器启动纪元（epoch），DSH 重启后页面自动重置游标，避免永久漏发。
- 会话绑定只认真人键盘输入（`user/message` 且 `source.kind==='user'`）——失败重试循环
  的会话事件流会永久霸占"最近活跃"绑定（实测踩坑），插件 steer 与后台事件不参与判定。

### 修正（相对阶段 0 规格的实测变更）

- **部署前提**：`dsh web --host 0.0.0.0` 被 DSH CLI 显式封禁（安全策略：避免把整个
  Agent 座舱暴露到网络；webserver config schema 亦只允许回环/0.0.0.0）。改为插件内置
  **路径过滤反向代理**：默认 `0.0.0.0:3090 → 127.0.0.1:3080`，仅放行 `/mobile-remote/*`，
  DSH 本体保持回环——比整站暴露更安全，手机地址由设置页自动给出。
- 审批等待窗可调档位为 30 秒/1/2/5 分钟（默认 120 秒），超时回落电脑端 GUI。

### 部署前提

- DSH web 正常启动即可（本体保持回环 127.0.0.1:3080）；手机可达性由插件中继承担，
  首次启用若被 Windows 防火墙拦截需放行 node 入站（局域网）。
